
class RegionUnit {
  int id;
  String name;
  String parentName;
  String parentId;
  int diagnosedCasesCount;

  RegionUnit({this.id, this.name, this.parentName, this.parentId, this.diagnosedCasesCount});
}